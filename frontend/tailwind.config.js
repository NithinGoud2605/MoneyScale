// tailwind.config.js
module.exports = {
  // Omit darkMode entirely or set to 'media' if you want system-based defaults
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
